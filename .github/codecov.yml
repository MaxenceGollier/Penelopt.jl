# Drops on the order 0.01% are typical even when no change occurs
# Having the threshold set a little higher (0.5%) than that makes it 
# a little more tolerant to fluctuations (source: Krylov.jl)
coverage:
  status:
    project:
      default:
        threshold: 0.5%
    patch:
      default:
        threshold: 0.5%

ignore:
  - "ext/PeneloptKrylovExt.jl"
  - "ext/Krylov"
  - "ext/PeneloptHSLExt.jl"
  - "ext/HSL"